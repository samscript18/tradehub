import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Checkout",
  description: "Checkout page",
  robots: {
    index: false,
    follow: false,
  },
};

const Page = () => {
  return <></>;
};

export default Page;
