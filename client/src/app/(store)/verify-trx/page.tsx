import { Metadata } from "next";
import VerifyTransaction from "../../../components/UI/Product/verify-transaction";

export const metadata: Metadata = {
  title: "Verify Transaction",
};

const Page = () => <VerifyTransaction />;

export default Page;
